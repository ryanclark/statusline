name: CI

on:
  pull_request:
  merge_group:
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    uses: ryanclark/workflows/.github/workflows/rust-ci.yml@v1
    with:
      codesigned-clippy: true
      cargo-deny: true
